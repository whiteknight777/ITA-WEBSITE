{% extends 'base-authentification.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <!-- Error404 content start -->
                <div class="error404-content">
                    <h1>Erreur 404</h1>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {# ... #}
                    {% endif %}
                    <h2>Oups.. Nous n'avons pas pu retrouver cette page.</h2>
                    <p>La page que vous recherchez à peut-être été déplacée, supprimée, renommée ou retirée du serveur.</p>
                    {# <form action="" method="post"> #}
                        <a href="{{ path('welcome') }}" type="submit" class="btn button-md button-theme">Retourner à la page d'Accueil</a>
                    {# </form> #}
                </div>
                <!-- Error404 content end -->
            </div>
        </div>
    </div>
{% endblock %}