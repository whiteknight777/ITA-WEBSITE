{% extends 'base-authentification.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <!-- Error404 content start -->
                <div class="error500-content">
                    <h1>Erreur 500</h1>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {# ... #}
                    {% endif %}
                    <h2>Oups.. Une erreur est survenue lors du chargement de la page.</h2>
                    <p>Veuillez contacter l'administrateur du serveur.</p>
                    {# <form action="" method="post"> #}
                        <a href="{{ path('welcome') }}" type="submit" class="btn button-md button-theme">Retourner à la page d'Accueil</a>
                    {# </form> #}
                </div>
                <!-- Error404 content end -->
            </div>
        </div>
    </div>
{% endblock %}